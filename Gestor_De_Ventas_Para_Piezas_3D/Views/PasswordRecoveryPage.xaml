<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Gestor_De_Ventas_Para_Piezas_3D.Vistas.PasswordRecoveryPage"
             BackgroundColor="#D0E0EF" 
             >

    <ScrollView>
        <VerticalStackLayout Padding="10" Spacing="15" VerticalOptions="Center">

            <!-- 1. LOGO CIRCULAR SUPERIOR -->
            <Frame HorizontalOptions="Center" 
       CornerRadius="85"       
       HeightRequest="180"      
       WidthRequest="300"       
       Padding="0" 
       IsClippedToBounds="True" 
       BorderColor="Transparent"
       BackgroundColor="Transparent" 
       HasShadow="False">

                <!-- Asegúrate de que tu imagen se llame así en Resources/Images -->
                <Image Source="logo_gestor_base_de_dato.png" 
           Aspect="AspectFit"
           HorizontalOptions="Center"
           VerticalOptions="Center"/>
            </Frame>


            <!-- 2. TARJETA BLANCA DEL FORMULARIO -->
            <Frame BackgroundColor="White" 
                   CornerRadius="20" 
                   Padding="20" 
                   HasShadow="True" 
                   BorderColor="Transparent">

                <VerticalStackLayout Spacing="20">

                    <!-- Campo 1: Usuario -->
                    <Grid ColumnDefinitions="50, *" ColumnSpacing="15">
                        <!-- Icono Grande Izquierda -->
                        <Label Grid.Column="0" Text="👤" FontSize="40" VerticalOptions="Center" HorizontalOptions="Center" TextColor="#1F2937"/>

                        <!-- Inputs Derecha -->
                        <VerticalStackLayout Grid.Column="1" Spacing="5">
                            <Label Text="Usuario" FontAttributes="Bold" TextColor="Black" FontSize="16"/>
                            <Frame BackgroundColor="#F3E8E2" CornerRadius="15" Padding="10,0" HeightRequest="45" BorderColor="Transparent">
                                <Entry Placeholder="Ej. usuario1" PlaceholderColor="Gray" TextColor="Black"/>
                            </Frame>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Campo 2: Nueva Contraseña -->
                    <Grid ColumnDefinitions="50, *" ColumnSpacing="15">
                        <Label Grid.Column="0" Text="🔒" FontSize="40" VerticalOptions="Center" HorizontalOptions="Center" TextColor="#1F2937"/>
                        <VerticalStackLayout Grid.Column="1" Spacing="5">
                            <Label Text="Nueva contraseña" FontAttributes="Bold" TextColor="Black" FontSize="16"/>
                            <Frame BackgroundColor="#F3E8E2" CornerRadius="15" Padding="10,0" HeightRequest="45" BorderColor="Transparent">
                                <Entry IsPassword="True" Placeholder="Ej. 1234567" PlaceholderColor="Gray" TextColor="Black"/>
                            </Frame>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Campo 3: Repita Contraseña -->
                    <Grid ColumnDefinitions="50, *" ColumnSpacing="15">
                        <Label Grid.Column="0" Text="🔒" FontSize="40" VerticalOptions="Center" HorizontalOptions="Center" TextColor="#1F2937"/>
                        <VerticalStackLayout Grid.Column="1" Spacing="5">
                            <Label Text="Repita contraseña" FontAttributes="Bold" TextColor="Black" FontSize="16"/>
                            <Frame BackgroundColor="#F3E8E2" CornerRadius="15" Padding="10,0" HeightRequest="45" BorderColor="Transparent">
                                <Entry IsPassword="True" Placeholder="Ej. 1234567" PlaceholderColor="Gray" TextColor="Black"/>
                            </Frame>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Campo 4: Código de validación -->
                    <Grid ColumnDefinitions="50, *" ColumnSpacing="15">
                        <!-- Icono simulando la insignia de verificación -->
                        <Label Grid.Column="0" Text="✅" FontSize="40" VerticalOptions="Center" HorizontalOptions="Center" TextColor="#1F2937"/>
                        <VerticalStackLayout Grid.Column="1" Spacing="5">
                            <Label Text="Código de validación" FontAttributes="Bold" TextColor="Black" FontSize="16"/>
                            <Frame BackgroundColor="#F3E8E2" CornerRadius="15" Padding="10,0" HeightRequest="45" BorderColor="Transparent">
                                <Entry Placeholder="Ej. 1234567" Keyboard="Numeric" PlaceholderColor="Gray" TextColor="Black"/>
                            </Frame>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Botón Ingresar -->
                    <Button Text="Ingresar"
                            BackgroundColor="#1F2937"
                            TextColor="White"
                            FontAttributes="Bold"
                            CornerRadius="20"
                            HeightRequest="50"
                            WidthRequest="150"
                            HorizontalOptions="Center"
                            Margin="0,10,0,0"
                            Clicked="BtnRecuperar_Clicked"/>

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>