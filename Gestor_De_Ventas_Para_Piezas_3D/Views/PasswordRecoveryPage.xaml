<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Gestor_De_Ventas_Para_Piezas_3D.Vistas.PasswordRecoveryPage"
             Title="Recuperar Contraseña"
             BackgroundColor="#D6E6F2">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15" VerticalOptions="Center">

            <!-- Cabecera (Opcional, para que no se vea vacío arriba) -->
            <Image Source="logo_gestor_base_de_dato.png" HeightRequest="80" HorizontalOptions="Center" Margin="0,0,0,20"/>

            <!-- Tarjeta Blanca -->
            <Frame BackgroundColor="White" CornerRadius="20" Padding="25" HasShadow="True" BorderColor="Transparent">
                <VerticalStackLayout Spacing="20">

                    <!-- Campo 1: Usuario -->
                    <VerticalStackLayout Spacing="5">
                        <Grid ColumnDefinitions="40, *">
                            <Label Text="👤" FontSize="24" VerticalOptions="Center" TextColor="#4A148C"/>
                            <Label Grid.Column="1" Text="Usuario" FontAttributes="Bold" TextColor="Black" FontSize="16" VerticalOptions="Center"/>
                        </Grid>
                        <Frame BackgroundColor="#F2EBE5" CornerRadius="10" Padding="10,0" HeightRequest="45" BorderColor="Transparent">
                            <Entry x:Name="txtUsuario" Placeholder="Ej. usuario1" PlaceholderColor="Gray" TextColor="Black" VerticalOptions="Center"/>
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Campo 2: Nueva contraseña -->
                    <VerticalStackLayout Spacing="5">
                        <Grid ColumnDefinitions="40, *">
                            <Label Text="🔒" FontSize="24" VerticalOptions="Center" TextColor="#F57C00"/>
                            <Label Grid.Column="1" Text="Nueva contraseña" FontAttributes="Bold" TextColor="Black" FontSize="16" VerticalOptions="Center"/>
                        </Grid>
                        <Frame BackgroundColor="#F2EBE5" CornerRadius="10" Padding="10,0" HeightRequest="45" BorderColor="Transparent">
                            <Entry x:Name="txtNuevaPass" Placeholder="Ej. 1234567" IsPassword="True" PlaceholderColor="Gray" TextColor="Black" VerticalOptions="Center"/>
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Campo 3: Repita contraseña -->
                    <VerticalStackLayout Spacing="5">
                        <Grid ColumnDefinitions="40, *">
                            <Label Text="🔒" FontSize="24" VerticalOptions="Center" TextColor="#F57C00"/>
                            <Label Grid.Column="1" Text="Repita contraseña" FontAttributes="Bold" TextColor="Black" FontSize="16" VerticalOptions="Center"/>
                        </Grid>
                        <Frame BackgroundColor="#F2EBE5" CornerRadius="10" Padding="10,0" HeightRequest="45" BorderColor="Transparent">
                            <Entry x:Name="txtRepitaPass" Placeholder="Ej. 1234567" IsPassword="True" PlaceholderColor="Gray" TextColor="Black" VerticalOptions="Center"/>
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Campo 4: Código de validación -->
                    <VerticalStackLayout Spacing="5">
                        <Grid ColumnDefinitions="40, *">
                            <Label Text="✅" FontSize="24" VerticalOptions="Center" TextColor="#4CAF50"/>
                            <Label Grid.Column="1" Text="Código de validación" FontAttributes="Bold" TextColor="Black" FontSize="16" VerticalOptions="Center"/>
                        </Grid>
                        <Frame BackgroundColor="#F2EBE5" CornerRadius="10" Padding="10,0" HeightRequest="45" BorderColor="Transparent">
                            <Entry x:Name="txtCodigo" Placeholder="Ej. 1234567" Keyboard="Numeric" PlaceholderColor="Gray" TextColor="Black" VerticalOptions="Center"/>
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Botón Ingresar (Cambiar clave) -->
                    <Button Text="Ingresar" 
                            Clicked="OnCambiarPasswordClicked" 
                            BackgroundColor="#1A237E" 
                            TextColor="White" 
                            CornerRadius="20"
                            HeightRequest="50"
                            WidthRequest="150"
                            HorizontalOptions="Center"
                            Margin="0,20,0,0"
                            FontAttributes="Bold"/>

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>