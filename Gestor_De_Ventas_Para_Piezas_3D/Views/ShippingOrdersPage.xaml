<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:Gestor_De_Ventas_Para_Piezas_3D.Modelos"
             x:Class="Gestor_De_Ventas_Para_Piezas_3D.Vistas.ShippingOrdersPage"
             Title="Órdenes de Envío"
             BackgroundColor="White">

    <Grid RowDefinitions="Auto, Auto, Auto, *">

        <!-- 1. ENCABEZADO -->
        <Grid Grid.Row="0" BackgroundColor="#5F8090" HeightRequest="100" Padding="20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Logo -->
            <Frame HorizontalOptions="Start" CornerRadius="85" HeightRequest="150" WidthRequest="150" Padding="0" IsClippedToBounds="True" BorderColor="Transparent" BackgroundColor="Transparent" HasShadow="False">
                <Image Source="logo_gestor_base_de_dato.png" Aspect="AspectFit" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Frame>

            <!-- Título Central -->
            <Frame Grid.Column="1" HorizontalOptions="Center" VerticalOptions="Center" BackgroundColor="White" Padding="30,10" CornerRadius="0">
                <Label Text="Órdenes de envío" FontSize="22" TextColor="Black" FontAttributes="Bold"/>
            </Frame>

            <!-- Texto Derecha -->
            <Label Grid.Column="2" Text="Generar orden de envío" TextColor="#5F8090" VerticalOptions="Start" HorizontalOptions="End" FontSize="12"/>
        </Grid>

        <!-- 2. TÍTULO SECCIÓN -->
        <Label Grid.Row="1" Text="Lista de pedidos finalizados" FontSize="20" HorizontalOptions="Center" TextColor="Black" Margin="0,15,0,10"/>

        <!-- 3. ENCABEZADO TABLA -->
        <Grid Grid.Row="2" ColumnDefinitions="40, 2*, 2*, 2.5*, 2*, 2.5*, 1.5*, 2*" BackgroundColor="#C4D6E2" Padding="10">
            <Label Grid.Column="0" Text="ID" FontAttributes="Bold" TextColor="#1F2937"/>
            <Label Grid.Column="1" Text="Cliente" FontAttributes="Bold" TextColor="#1F2937"/>
            <Label Grid.Column="2" Text="Producto" FontAttributes="Bold" TextColor="#1F2937"/>
            <Label Grid.Column="3" Text="Fecha de entrega" FontAttributes="Bold" TextColor="#1F2937"/>
            <Label Grid.Column="4" Text="Estado" FontAttributes="Bold" TextColor="#1F2937"/>
            <Label Grid.Column="5" Text="Fecha de inicio" FontAttributes="Bold" TextColor="#1F2937"/>
            <Label Grid.Column="6" Text="Duración" FontAttributes="Bold" TextColor="#1F2937"/>
            <Label Grid.Column="7" Text="Órdenes de envío" FontAttributes="Bold" TextColor="#1F2937" HorizontalTextAlignment="Center"/>
        </Grid>

        <!-- 4. LISTA DE PEDIDOS COMPLETADOS -->
        <CollectionView Grid.Row="3" x:Name="cvEnvios" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:Venta">
                    <Grid ColumnDefinitions="40, 2*, 2*, 2.5*, 2*, 2.5*, 1.5*, 2*" Padding="10" RowDefinitions="Auto, Auto">

                        <BoxView Grid.Row="1" Grid.ColumnSpan="8" HeightRequest="1" Color="LightGray" Margin="0,5"/>

                        <!-- ID -->
                        <Label Grid.Row="0" Grid.Column="0" Text="{Binding Id}" VerticalOptions="Center" TextColor="Black"/>

                        <!-- Cliente -->
                        <Label Grid.Row="0" Grid.Column="1" Text="{Binding Cliente}" VerticalOptions="Center" TextColor="Black"/>

                        <!-- Producto -->
                        <Label Grid.Row="0" Grid.Column="2" Text="{Binding Producto}" VerticalOptions="Center" TextColor="Black"/>

                        <!-- Fecha Entrega (Fondo Gris) -->
                        <Frame Grid.Row="0" Grid.Column="3" BackgroundColor="#E5E7EB" Padding="8,2" CornerRadius="10" HorizontalOptions="Start" BorderColor="Transparent" HasShadow="False">
                            <Label Text="{Binding FechaEntrega}" FontSize="12" TextColor="Black"/>
                        </Frame>

                        <!-- Estado -->
                        <Label Grid.Row="0" Grid.Column="4" Text="{Binding Estado}" VerticalOptions="Center" TextColor="Black"/>

                        <!-- Fecha Inicio (Fondo Gris) -->
                        <Frame Grid.Row="0" Grid.Column="5" BackgroundColor="#E5E7EB" Padding="8,2" CornerRadius="10" HorizontalOptions="Start" BorderColor="Transparent" HasShadow="False">
                            <Label Text="{Binding FechaSolicitud}" FontSize="12" TextColor="Black"/>
                        </Frame>

                        <!-- Duración con Icono -->
                        <HorizontalStackLayout Grid.Row="0" Grid.Column="6" Spacing="5" VerticalOptions="Center">
                            <Label Text="📅" FontSize="16" TextColor="#7B68EE"/>
                            <Label Text="{Binding Duracion}" TextColor="Black" FontSize="12"/>
                        </HorizontalStackLayout>

                        <!-- Botón Imprimir -->
                        <Button Grid.Row="0" Grid.Column="7" 
                                Text="Imprimir" 
                                BackgroundColor="#2C3E50" 
                                TextColor="White" 
                                FontSize="12" 
                                CornerRadius="5" 
                                HeightRequest="35" 
                                Padding="0"
                                Clicked="BtnImprimir_Clicked"
                                CommandParameter="{Binding .}"/>

                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>